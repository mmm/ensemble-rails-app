ensemble: formula
name: rails-app
revision: 10
summary: "Ensemble formula to install Ruby on Rails"
description: "Ensemble formula to install Ruby on Rails"
requires:
  db:
    interface: mysql
provides:
  website:
    interface: http
